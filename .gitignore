# deps
node_modules/

# local env
*.local

# aider
.aider*

# eslint cache
.eslintcache

# build output
dist/
# runtime logs (local-only)
copilot-api.log
logs/

# npm lockfile (upstream does not track one)
package-lock.json
